pre-commit:
    parallel: false
    jobs:
        # - name: 🤖 Check branch name 🤖
        #   run: npm run validate-branch-name
        - name: "🧹 Biome lint & format 🧹"
          run: npm run biome:check-staged
